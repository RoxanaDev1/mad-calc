import React from "react";
import ReactDOM from "react-dom/client";

const root = ReactDOM.createRoot(document.getElementById("appContainer") as HTMLElement);
root.render(
  <React.StrictMode>
    <div>"Hello World - Frontend"</div>
  </React.StrictMode>
);
